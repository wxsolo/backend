{{- include ../conn/header}}
{{- include ../conn/navbar}}
<div  class="content row">
    <div class="col-md-3">
    </div>
    <div class="col-md-6 row">
    <ul class="bk-list">
            <li>
                <img src="{{= book.image }}" class="" alt="">
                <div class="content">
                    <p>{{= book.author }}</p>
                    <p>{{= book.price }}</p>
                    <p>{{= book.pages }}</p>
                    <p>{{= book.publisher }}</p>
                    <p>{{= book.summary }}</p>
                    <p>{{= book.pubdate }}</p>
                </div>
                <div class="clear">
                </div>
            </li>
        </ul>
        <div class="col-md-3"></div>
    </div>
</div>
{{- include ../conn/footer}}
